import styled from 'styled-components';
import {BTTO_LIGHT_GREY} from '../../../common-styles/vars';

export const TimezoneCellWrapper = styled.div`
    margin-top: auto;
    margin-bottom: auto;
    padding-left: 15px;
    color: ${BTTO_LIGHT_GREY};
    font-size: 14px;
    font-weight: 0.5;
`